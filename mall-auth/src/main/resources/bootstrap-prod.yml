spring:
  cloud:
    nacos:
      discovery:
        server-addr: http://nacos-registry:18848
      config:
        server-addr: http://nacos-registry:18848
        file-extension: yaml
logging:
  level:
    root: info